'use strict';

describe('chessApp.home module', function() {

  beforeEach(module('chessApp.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('HomeCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});