'use strict';

describe('chessApp.rating module', function () {

    beforeEach(module('chessApp.rating'));

    describe('rating controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view2Ctrl = $controller('RatingCtrl');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});